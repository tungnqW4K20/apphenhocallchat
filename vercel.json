{
  "framework": "vite",
  "buildCommand": "cd frontend-web && npm install --legacy-peer-deps --no-audit && npm run build",
  "outputDirectory": "frontend-web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
